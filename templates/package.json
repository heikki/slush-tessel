{
  "name": "<%- slug %>",
  "version": "0.0.1",
  "private": true,
  "main": "index.js",
  "dependencies": {<% dependencies.forEach(function(dependency, index, arr) { %>
    "<%= dependency %>": "*"<% if (index < arr.length - 1) { %>,<% } %><% }) %>
  },
  "hardware": {}
}
